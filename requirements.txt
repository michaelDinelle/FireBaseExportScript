# Firebase Export Script Requirements
# Core Firebase Admin SDK
firebase-admin>=6.2.0

# Required dependencies for Firebase Admin SDK
google-cloud-firestore>=2.11.0
google-cloud-storage>=2.9.0
google-auth>=2.17.0
google-api-core>=2.11.0
google-cloud-core>=2.3.0

# Additional dependencies that may be needed
grpcio>=1.53.0
protobuf>=4.21.0
requests>=2.28.0
urllib3>=1.26.0

# For async functionality (included in Python 3.7+, but listed for clarity)
# asyncio is built-in to Python 3.7+

# Optional: for better error handling and logging
colorlog>=6.7.0